# {{ ansible_managed }}
# oxlorg.graylog

[Unit]
Description=Timer to back-up Graylog

[Timer]
OnCalendar={{ GL_CONFIG.backup.timer }}
Persistent=false
WakeSystem=false

[Install]
WantedBy=multi-user.target
